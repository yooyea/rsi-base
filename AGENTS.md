# 本仓库工作入口

定位保持为：**指导项目通过「可靠的工程化」保障自我迭代。**

## 内容边界

[README.md](README.md)说明定位与使用；[SKILL.md](SKILL.md)负责执行入口；[工程保障章节](chapters/engineering/README.md)组织准则。

保持「SKILL.md → 章节 → 准则类别 → 单条准则」的层次。案例与外部研究放在 references 或 examples，不与项目必须遵循的规则混写。

每条准则说明保障目标、适用条件、工程落实、有效性验证和边界。具体项目按实际风险选择载体，不把 rsi-base 扩成脚手架、评分平台或通用编排服务。

新增内容前检查现有内容，优先修订与合并，避免同一要求多份定义。工程边界、类型和字段精确使用；本仓库不引入新旧双轨、历史兼容或猜测性 fallback。

## 事实与权限

引用仓库实践时记录固定提交、文件或运行来源，区分源码设计、实际运行、实际阻断和无法观察的内部流程。bot 身份不等于代码由 AI 编写。

工作流、检查脚本、验收规则的变化要说明保障是否改变，并验证正反例。不得通过削弱验证换取通过。未经授权不修改 GitHub 规则集、绕过审查、强制推送或合并 PR。

不把私有项目细节、账户数据或凭据写入公开案例。

## 验证与交付

修改内容后执行：

```sh
python3 -m unittest discover -s tests -v
python3 scripts/check_docs.py
```

这些检查不评估文档语义，也不证明业务项目能力。报告本地与 CI 结果时说明实际版本和范围，不把已提交、已开 PR、已配置 CI 说成已合并或门禁已启用。

本次变更存在跨会话未完成事项时，写到 PR 的任务与验证记录中。后续继续从仓库及 PR 读取当前事实。

## 对自己生效

维护本仓库时同样使用 [SKILL.md](SKILL.md)，按[自身保障记录](docs/self-bootstrap.md)
检查改动影响。新增准则必须从入口可达，必要章节不能只有标题。
涉及检查器、工作流及门禁变更时，复核正反例，并执行：

```sh
python3 scripts/check_repository_rules.py --repository yooyea/rsi-base
```

该命令只有读取权限；无法完成的平台核对必须保留未验证状态。
同一条断言不能通过修改检查器或期望规则来自证。敏感规则的变更需要独立于当前
实现意图的用户授权与审查；单人仓库不虚构独立 reviewer，不自动申请或扩大权限。
